apply plugin: 'java'

repositories {
	mavenCentral()
}

sourceSets {

	main {
		java {
			srcDir 'src'
		}
	}	
}

dependencies {

	compile group: 'com.google.code.gson', name: 'gson', version: '2.8.6'
	
}